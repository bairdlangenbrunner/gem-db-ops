# Postgres pulls (lng/, gogpt/, goget/) need these.
sqlalchemy>=2.0
psycopg2-binary
requests

# The Sheets pulls (ggit/, goit/, gem_sheets.py) need NO pip packages — they
# shell out to the `gws` CLI (Google Workspace CLI, `brew install`) against the
# read-only work profile at ~/.config/gws-gem. gem_colmap.py is stdlib-only.
